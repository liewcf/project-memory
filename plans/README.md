# Implementation Plans

This backlog contains five numbered implementation plans plus this coordination
file (six tracked backlog files total). It was generated by the `improve` skill
on 2026-07-14 at commit `0766747`.

Reconciled on 2026-07-27 against the current worktree based at `c4da8d1`.
No implementation commit was created because none was requested.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Establish a complete verification baseline | P1 | S | — | DONE |
| 002 | Reject symlinked repair targets | P1 | M | 001 | DONE |
| 003 | Make the frontmatter boundary strict and truthful | P1 | M | 001 | DONE |
| 004 | Validate metadata semantics | P2 | S | 001 | DONE |
| 005 | Add a repository-content trust boundary | P2 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency notes

- Plan 001 establishes the executable test baseline before parser and filesystem changes.
- Plans 002–004 depend on 001 because their new regression tests must run through the documented verification commands.
- Plan 005 is documentation and contract coverage only, so it can run independently.

## Reconciliation evidence

- Repo suite: 106 tests passed.
- Package smoke suite: 1 test passed.
- All bundled scripts passed cache-safe `py_compile`.
- Repo-local Project Memory Metadata v1 files passed validation.
- `git diff --check` passed and `project-memory/SKILL.md` remained at 100 lines.
- Plan 002's descriptor-relative no-follow path was verified on Darwin 25.5.0
  with Python 3.14.6. Runtimes without the required primitives now fail
  explicitly instead of using an unsafe fallback.

## Findings considered and rejected

- Atomic writes: valid follow-up work, but outside the default top-five selection; do not expand these plans to include it.
- Python minimum-version documentation: valid low-impact follow-up; keep it separate from these plans.
- Implicit invocation/token reduction: direction option, not a confirmed defect; do not change `allow_implicit_invocation` here.
- Policy deduplication: repeated public, active, deferred, generated, and test surfaces are intentional; no concrete drift was found.
- Plugin Eval complexity warning: the current analyzer counted one Python function across nine files, so the metric is not reliable enough to drive a code change.
